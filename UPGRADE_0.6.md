# 升級至 0.6.2

0.6.2 保留 0.6.1 的期權鍊綠色標示修正，並統一時間顯示。一般工作表時間依「顯示時區」呈現，預設為台北時間；欄位名稱含 `(UTC)` 的系統稽核時間仍保留 UTC。

## 升級步驟

1. 將最新版 `apps-script/Code.gs` 與 `apps-script/Index.html` 更新至綁定 Google Sheet 的 Apps Script 專案並儲存。
2. 重新整理 Google Sheet。
3. 執行「選擇權警示 → 初始化／升級系統」。
4. 確認「設定」分頁的「系統版本」顯示 `0.6.2`，且 Google Sheet 的檔案時區已設為 `Asia/Taipei`。
5. 確認已出現「收盤Delta趨勢」分頁，第一列具有 40 個欄位。
6. 下一次正式收盤自動更新成功後，確認每個 `ALL` 掃描新增 6 列；只顯示 CALL 或 PUT 的掃描新增 3 列。

執行初始化時，系統會把既有期權鍊、掃描設定及收盤趨勢中的一般 ISO 時間文字轉成日期時間儲存格；明確標示 `(UTC)` 的欄位不會被轉成台北時間。
日後若修改「顯示時區」，請再執行一次「初始化／升級系統」，讓 Google Sheet 檔案時區同步更新。

## 資料規則

- 只在自動收盤更新寫入；盤外或盤中的手動更新不新增歷史列。
- 每個「美東交易日＋掃描ID＋CALL/PUT＋目標 Delta」只有一個快照 ID。
- 選取規則為 `|Delta| ≤ 目標值`，再取最接近目標者；同值時依較小 Spread、較高未平倉量排序。
- 年化報酬率仍只使用有效 Bid。PUT 本金為履約價；CALL 本金為掃描設定的持股成本。
- 無符合合約、Greeks 不足或抓取失敗會保留狀態列，不會寫成 0。
- 已成功選取的每日快照不會被後續執行覆蓋；失敗占位列可由同一收盤時段的成功重試補齊。

## 注意事項

- 此功能只會從升級後開始累積，不會嘗試回補 Yahoo 已不存在或無法驗證的過往 Bid／Ask。
- Yahoo／yfinance 可能延遲或缺漏；Greeks 仍為 Black-Scholes-Merton 模型估算值。
- 如果 GitHub Actions 顯示「收盤Delta趨勢欄位與目前版本不一致」，請再次執行「初始化／升級系統」後重跑。
