[project]
name = "option-alerts"
version = "0.5.1"
description = "Yahoo Finance option alerts synchronized to Google Sheets"
requires-python = ">=3.11"

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["."]
